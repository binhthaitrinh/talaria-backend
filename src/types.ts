export interface socialMedia {
  website: string;
  link: string;
}

export interface bankAcct {
  bankName: string;
  acctNumber: string;
  bankLocation?: string;
}

export interface commissionRate {
  website: string;
  rate: number;
}
